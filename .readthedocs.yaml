version: 2

sphinx:
  configuration: docs/conf.py
  builder: html

formats: all

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - all
    - requirements: docs/requirements_docs.txt

submodules:
  include: all
